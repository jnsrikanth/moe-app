node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables
.env
.env.local
.env.development
.env.development.local
.env.test.local
.env.production.local

# Local data and artifacts
/data/
uploaded-datasets/
*.db
*.db-shm
*.db-wal

# Development files
.dev-server.pid

# Yarn Berry
# Ignore all of .yarn except the pieces needed for zero-installs
.yarn/*
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
!.yarn/cache
!.yarn/patches

# Yarn PnP artifacts (we're using nodeLinker: node-modules, but ignore if present)
.pnp.*
