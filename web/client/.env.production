# Production Environment Variables for Cloud Run build
# For Cloud Run (single service serving API + UI), leave these empty to use same-origin paths.

# Backend API URL
VITE_BACKEND_URL=

# WebSocket URL  
VITE_WS_URL=
